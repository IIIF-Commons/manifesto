
module Manifesto {
    export class Canvas {
        height: number;
        label: string;
        ranges: Range[];
        width: number;
        //getRange() {
        //    return M.getCanvasRange(this);
        //}
    }
}
